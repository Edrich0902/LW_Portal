# Deprecated

This is the **legacy Svelte admin portal**, replaced by **`LW_Portal_2.0`** (Vue 3 + TypeScript).

- Do not add features or fix bugs here unless explicitly asked for a hotfix on the old deployment.
- All new portal work belongs in `LW_Portal_2.0/`.
- Shared backend: `../supabase/`.
